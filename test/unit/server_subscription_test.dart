import 'package:test/test.dart';

//@GenerateMocks([ISpinify])
void main() {
  group('SpinifyServerSubscription', () {
    test('SpinifyServerSubscription', () {
      //final client = FakeSpinify();
      //when(client.config).thenReturn(SpinifyConfig());
      /* final subscription = SpinifyServerSubscription(client, 'channel');
      expect(subscription.channel, 'channel');
      expect(subscription.recoverable, isTrue);
      expect(subscription.epoch, isNotNull);
      expect(subscription.offset, isNotNull); */
    });
  });
}
