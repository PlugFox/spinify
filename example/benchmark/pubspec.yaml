name: spinifybenchmark
description: "Spinify Benchmark"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.4.0
  flutter: ^3.24.0

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8

  # Centrifuge clients
  centrifuge: ^0.15.0
  spinify:
    path: ../../

  # Utilities
  l: ^5.0.0-pre.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
